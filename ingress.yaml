apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  creationTimestamp: null
  name: react2
  namespace: thitipun-react
spec:
  rules:
  - host: ken-ubuntu.saksiam.corp
    http:
      paths:
      - backend:
          service:
            name: react2
            port:
              number: 3000
        path: /react2
        pathType: Prefix
status:
  loadBalancer: {}
